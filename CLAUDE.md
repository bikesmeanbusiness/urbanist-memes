# urbanist-memes

## Git

This repo is under the `bikesmeanbusiness` GitHub org, which uses a separate SSH key.
Remote URL must use `github-bmb` host alias (not `github.com`):

```
git@github-bmb:bikesmeanbusiness/urbanist-memes.git
```
